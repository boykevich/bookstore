@page "/"
@using Microsoft.AspNetCore.Components

<PageTitle>Home</PageTitle>

<!-- Top Navigation Bar -->
<div class="top-bar">
    <div class="logo">
        <h2>BookStore</h2>
    </div>
    <div class="search-bar">
        <InputText @bind-Value="SearchQuery" placeholder="Enter desired book..." class="search-input" />
        <button @onclick="PerformSearch" class="search-button">🔍 Search</button>
    </div>
    <div class="cart">
        <a href="/cart">
            <span class="cart-icon">🛒</span> <span class="cart-count">0</span>
        </a>
    </div>
</div>

<!-- Main Content -->
<div class="main-content">
    @* <h1>Hello, world!</h1> *@
    <p>Welcome to your new app.</p>
</div>

@code {
    private string SearchQuery { get; set; } = "";

    private void PerformSearch()
    {
        Console.WriteLine($"Searching for: {SearchQuery}");
        // Here you can implement navigation to a search results page
        // NavigationManager.NavigateTo($"/search?query={SearchQuery}");
    }
}
